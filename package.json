{
  "name": "qraft",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn workspaces foreach --verbose --recursive --no-private --from '@radist2s/*' run build"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "packageManager": "yarn@4.0.2"
}
